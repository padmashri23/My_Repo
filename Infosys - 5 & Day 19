#Maximum Possible Sum of Products
class Solution:

    def MaxSum(self, A, B, N):
        A.sort()
        B.sort()
        max_sum=0
        for i in range(N):
            max_sum+=A[i]*B[i]
        return max_sum
